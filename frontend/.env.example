# Backend API base URL including version prefix (no trailing slash)
VITE_API_URL=http://localhost:4010/v1

# Telegram bot username (without @) used for the Login Widget
VITE_TG_BOT_NAME=your_bot_name
